# Fixture: autox run with a moved start anchor and an optimized run-in.
# Values are deliberately non-default so the assertions prove the YAML was
# read, not that the dataclass defaults happen to match.
pipeline:
  mode: autox
  model_name: four_wheel
  autox:
    lead_in_m: 8.0
    ocp_lead_m: 2.5
    start_x: 12.5
    start_y: -3.25
    start_node_offset: 3
