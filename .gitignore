__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/
.env
.idea/
.vscode/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.editorconfig
dist/
build/
*.egg-info/

# Research code. Allowlist, same reasoning as the tracks below: the four files
# named here are parameterised and run on shipped data; everything else in
# research/ is a one-off study tied to solutions and tracks that stay local.
research/**/*.py
!research/experiments/skidpad_batch.py
!research/experiments/autox_dfx_grip_sweep.py
!research/experiments/animate_lap.py
!research/experiments/interactive_lap.py

# Track data. Allowlist rather than blocklist, so the default for anything
# dropped into data/tracks/ is "stays on this machine" -- a boundary CSV off a
# test day is the team's, not the repo's. The shipped set is the four public
# event tracks plus what the generators produce, which is all a fresh clone
# needs. gitignore does not govern already-tracked files, so anything added
# here later also has to leave the index with `git rm --cached`.
data/tracks/*.csv
!data/tracks/fscz_2025.csv
!data/tracks/fscz_2026.csv
!data/tracks/fsg_autox_2025.csv
!data/tracks/fsg_autox_2026.csv
!data/tracks/ellipse.csv
!data/tracks/bean.csv
!data/tracks/fsg_random.csv

# Data / results
data/raw/
data/interim/
data/processed/
data/discretized/
data/solutions/
data/output_trajectories/
data/validation/
data/experiments/
data/sensitivity/
ocp_plots/
results/
todo-list.md

logs/

# Working-tree scratch: figures written next to the code that made them.
/*.png
eng_design/
.claude/
